# Obsolete. This repository no longer uses .env for provider credentials.
# Keep secrets in profiles.json instead, starting from profiles.json.template.
# This file remains only as a compatibility marker for older local setups.
